- name: multiple packages install in ansible
  hosts: localhost
  tasks:
  - name: install the packages
    ansible.builtin.package:
      name: "{{item}}"
      state: present
    loop:
    - git
    - maven
      